{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    {% if translations is defined %}
        <ul>
            {% for translation in translations %}
                <li>{{ translation }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}